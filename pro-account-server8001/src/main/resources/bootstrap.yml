server:
  port: 8001
spring:
  application:
    name: account-server
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
  rabbitmq:
    password: shane
    username: shane
    host: localhost
    port: 5672
  thymeleaf:
    cache: false

management:
  endpoints:
    web:
      exposure:
        include: '*'
